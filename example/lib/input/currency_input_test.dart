import 'package:flutter/material.dart';

class CurrencyInputTest extends StatelessWidget {
  const CurrencyInputTest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
